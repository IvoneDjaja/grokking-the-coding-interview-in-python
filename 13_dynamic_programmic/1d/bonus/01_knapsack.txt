def maxKnapsack(capacity: int, values: List[int], weights: List[int]) -> int:
	total = sum(values)
	dp = (total + 1) * [0]
	
	for i in range(len(weights)):
		capacity = 0
		value = 0
		for j in range(total, 0, -1):
			capacity += weights[i]
			if capacity > maxCapacity:
				break
			value += values[i]
		dp[i] = max(dp[i], value)
