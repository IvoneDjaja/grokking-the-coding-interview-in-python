def uniquePaths(m: int, n: int) -> int:

	def dfs(i, j):
		if i < 0 or i >= m or j < 0 or j >= n:
			return 0
		if i == m and j == n:
			return 1
		return dfs(i + 1, j) + dfs(i, j + 1)
		
	return dfs(0, 0)
