class TrieNode:
	def __init__(self):
		self.charMap = {}
		self.isEnd = False

class WordDictionary:
def __init__(self):
self.root = TrieNode()

	def addWord(self, word):
		current = self.root
for char in word:
if char not in current.charMap:
	current = TrieNode()
current = current.charMap[char]
		current.isEnd = True

	def searchWord(self, word):
		n = len(word)
		current = self.root
		def dfs(index, node):
			if index == n:
				return True
			if word[index] in self.charMap:
				return dfs(index + 1, word[index])
			if word[index] == '.':
				for key in charMap:
					if dfs(index + 1, word[key])
return True 		
return False
		return dfs(0, self.root)